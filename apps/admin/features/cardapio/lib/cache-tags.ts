export function tagCardapioCatalogo(empresaId: string): string {
  return `cardapio-catalogo-${empresaId}`
}

export function tagCardapioDias(empresaId: string): string {
  return `cardapio-dias-${empresaId}`
}
